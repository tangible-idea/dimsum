project_id = "dumplings"

[functions.clicker_device_auth]
verify_jwt = false   # 로그인 전(진입 단계)에도 호출되므로 내부에서 직접 검증

[functions.clicker_device_register]
verify_jwt = false   # 내부에서 Authorization 헤더로 유저 검증

[functions.clicker_poke]
verify_jwt = false   # 유저 JWT 대신 x-device-secret으로 기기 인증

[functions.clicker_poke_inbox]
verify_jwt = false   # 유저 JWT 대신 x-device-secret으로 기기 인증
